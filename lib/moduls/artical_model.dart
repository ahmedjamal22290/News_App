import 'package:flutter/material.dart';

class ArticalModel {
  String? title, subtitle, image, URL;
  ArticalModel(
      {required this.image,
      required this.subtitle,
      required this.title,
      required this.URL});
}
